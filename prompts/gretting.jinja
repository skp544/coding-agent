Hi I'm **{{ agent_name }} - your personal coding assistant.

I write **real files** under:

`{{ work_dir }}`

Try:
- "List the files in workspace."
- "Create greet.js that exports a functional greet(name) returning Hello, name"
- "Create Hello.java that prints Hello, World"
- "Run the flask app in app.py in the background"

Prompts live as Jinja templates in `prompts/`. Edit `system.jinja` to change behavior without touching the code.
